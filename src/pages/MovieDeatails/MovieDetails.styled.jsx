import { NavLink } from 'react-router-dom';

import styled from 'styled-components';

export const AdditionalInfo = styled.div``;
export const NavListInfo = styled.ul``;
export const NavLinkInfo = styled(NavLink)``;
